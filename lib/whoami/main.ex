defmodule Whoami.Main do
  use GenServer
  @moduledoc """
  Starts and orchestrates the game. The game
  consists of multiple processes: the server, 
  which holds state, and the processes assigned
  to each player, which will display the info 
  and options to each player.
  """

  def init() do

  end
end
